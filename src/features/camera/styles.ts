import { motion } from "motion/react";
import { styled } from "styled-components";

export const StyledCamera = styled(motion.div)`
  position: absolute;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  top: 0;
  left: 0;
`;
