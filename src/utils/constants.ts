import { type ThemeName } from "@/styles/themes";

export const DEFAULT_THEME: ThemeName = "defaultTheme";

export const PIXEL_SIZE = 3;

export const MONITOR_RESOLUTION = {
  width: 1024 / PIXEL_SIZE,
  height: 768 / PIXEL_SIZE,
};
